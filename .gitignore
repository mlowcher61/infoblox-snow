# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/

# Ansible
*.retry
.ansible/
collections/ansible_collections/
roles/external/

# IDE / OS
.vscode/
.idea/
.DS_Store

# Local credential files — NEVER COMMIT.
# (We don't use vault; this is here as a safety net in case someone
# tries to add a vault file or .env "just for testing".)
*.vault
.env
.env.*
!.env.example
secrets.yml
*_credentials.yml
*_secrets.yml

# Downloaded CSVs from local testing
/tmp/snow_request_*.csv
